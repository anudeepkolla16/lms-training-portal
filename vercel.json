{
  "buildCommand": "npm --prefix lms-training-portal install && npm --prefix lms-training-portal run build",
  "outputDirectory": "lms-training-portal/build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
